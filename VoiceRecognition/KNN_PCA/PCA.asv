function [outputArg1] = PCA(inputArg1, numOfFiles)
X = inputArg1;
% 零均值化
X_zeroMean = X - mean(X);
% 协方差矩阵
C = 1/numOfFiles *(X * X');
[V, D] = eig(C);
[d,ind] = sort(diag(D),'descend');
Ds = D(ind,ind);
Vs = V(:,ind);
outputArg1 = Vs(:, 1) * X;
end

