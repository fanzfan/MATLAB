%% mu = 0.9
mu = [0.9 0.8 0.7 0.5];
for mu1 = mu
    % 读取全部文件
    num = 128;
    fftPoints = 1024;
    voice_fft = zeros(fftPoints, numOfFiles);
    % 读取并作fft
    for i = 1 : num
        voice_fft(:, i) = fft(audioread(['C:\Users\Vela\OneDrive\桌面\语音信号处理\KNN\数据\训练样本\' num2str(i) '.wav']), fftPoints);
    end
    voice_PCA = PCA(abs(voice_fft), num, mu1);
    [numOfVec, ~]
end